{
  "name": "@agent-kernel/mcp",
  "version": "2.1.0",
  "description": "MCP server for Agent Kernel metacognition - knowledge retrieval + protocol orchestration. Ships with bundled principles, skills, and commands.",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "agent-kernel-mcp": "./bin/cli.js"
  },
  "files": [
    "build",
    "resources",
    "bin"
  ],
  "scripts": {
    "build": "tsc",
    "bundle": "node scripts/bundle-resources.js",
    "prepublish": "npm run build && npm run bundle",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "mcp",
    "claude",
    "agent-kernel",
    "metacognition",
    "ai-agent"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/anthropics/agent-kernel-mcp"
  },
  "license": "MIT"
}
